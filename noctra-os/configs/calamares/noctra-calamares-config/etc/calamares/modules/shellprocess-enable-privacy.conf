dontChroot: false
timeout: 30

script:
  - command: "systemctl enable NetworkManager sddm tor nftables"
    timeout: 30
  - command: "if command -v noctra-network-mode >/dev/null 2>&1; then noctra-network-mode tor-locked || true; fi"
    timeout: 30

